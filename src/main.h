#ifndef MAIN_H
#define MAIN_H
#include <stdbool.h>

// Funktionsdeklarationen
void start();

// Externe Variablen, da ihre Werte in main.c definiert werden.
extern bool gameOpen;
extern unsigned char current_room;

#endif